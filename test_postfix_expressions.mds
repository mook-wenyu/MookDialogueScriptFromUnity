node: test_postfix
---
Player: Let's test the new postfix expressions!

// 测试函数调用
<<set $result = random(1, 10)>>
Player: Random result is {$result}

// 测试成员访问（如果实现了对象支持）
<<if $player.level > 5>>
Player: My level is high enough!
<<endif>>

// 测试索引访问
<<set $array = "hello">>
Player: First character is {$array[0]}

// 测试链式调用
<<set $complex = someObject.method(1, 2).property[0]>>
Player: Complex result: {$complex}
===